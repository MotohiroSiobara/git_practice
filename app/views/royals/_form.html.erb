<%= form_for(@royal) do |f| %>
  <% if @royal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@royal.errors.count, "error") %> prohibited this royal from being saved:</h2>

      <ul>
      <% @royal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
